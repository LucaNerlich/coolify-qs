[toolchain]
# Pinned so the bundled backend is byte-for-byte reproducible. Bump this in
# the same commit that regenerates omarchy/bin/coolify-qs.
channel = "1.97.1"
targets = ["x86_64-unknown-linux-musl"]
